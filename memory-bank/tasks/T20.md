# T20: Migrate viewer to quantumofgravity.com

**Status:** ✅ Completed
**Priority:** HIGH
**Started:** 2026-07-07
**Completed:** 2026-07-07 12:35 IST

## Description

Moved cron-digests viewer from GitHub Pages to self-hosted Apache server at `quantumofgravity.com/cron-digests/`.

## Why

User wanted the digests under their own domain rather than GitHub Pages. Server is already running Apache on the VPS.

## What Was Done

1. Identified Apache vhost DocumentRoot: `/home/quantumofgravity/public_html`
2. Created `/home/quantumofgravity/public_html/cron-digests/` directory
3. Copied all `viewer/*` contents:
   - `index.html` (main viewer)
   - `index.json` (digest index)
   - `index.db` (SQLite index)
   - `arxiv/` (digest files)
   - `web-science/` (digest files)
   - `moltbook/` (digest files)
4. Set ownership: `chown -R quantumofgravity:quantumofgravity`
5. Verified all endpoints:
   - `https://quantumofgravity.com/cron-digests/` → 200 OK
   - `.../index.json` → 200 OK
   - `.../arxiv/2026-07-07.md` → 200 OK

## Files Changed

- `/home/quantumofgravity/public_html/cron-digests/` (new directory, all contents)

## Notes

- GitHub Pages deployment remains active as backup
- CI workflow (`ci.yml`) still deploys to GitHub Pages on push
- Server deployment is currently manual (`cp -r`); could be automated with CI rsync step
- Apache config already allows `.htaccess` overrides if needed for SPA routing

## Next Steps

- T21: Modularize viewer + add calendar view
- T22: Fix Moltbook empty entries (after redesign)
