# T22: Fix Moltbook empty entries (API 401)

**Status:** ⏸️ Paused (blocked on T21)
**Priority:** MEDIUM
**Started:** 2026-07-07
**Dependencies:** T21

## Description

Moltbook research stream digests since approximately June 25 are 6-line stubs with "Items found: 0" and no actual content. The API appears to be returning 401 Unauthorized responses.

## Symptoms

- Files like `moltbook/2026-06-25.md` through `moltbook/2026-07-07.md` are all ~6 lines
- Content: just header + "Items found: 0" + "Generated:" timestamp + "---"
- Earlier files (e.g., `2026-06-07.md`, 8,522 bytes, 9 entries) have real content
- Moltbook personal stream cron shows 401 errors in logs

## Root Cause (Hypothesis)

API authentication token expired or Moltbook service changed their auth requirements. The cron job continues to run and create empty files because it doesn't check for API failure before writing.

## User Stance

User explicitly said:
> "I don't want empty entries to be hidden. I don't see why any of the entries should be empty. I'm sure Moltbook has enough content for a daily digest"

This means: **fix the source, don't hide in UI.**

## Acceptance Criteria

- [ ] Diagnose API 401 cause (token, URL, service status)
- [ ] Fix authentication or endpoint issue
- [ ] Regenerate all empty Moltbook digests (June 25–July 7)
- [ ] Add guard in generator: skip writing if API returns 0 items or error
- [ ] Verify daily digests have actual content going forward

## Investigation Steps

1. Check Moltbook cron job payload — what endpoint is it calling?
2. Check `~/.openclaw/logs/moltbook-research.md` — does it have recent entries?
3. Test API endpoint manually with current credentials
4. Check if token needs refresh or if service URL changed
5. Update cron job prompt if API requirements changed

## Files to Check

- Moltbook cron job config
- `scripts/generate-moltbook-digest.js`
- `~/.openclaw/logs/moltbook-research.md`
- `moltbook/` directory (to identify last good digest date)

## Notes

- Phase 1 in user's specified order (after Phase 3)
- The empty files should NOT be deleted — they indicate days where the pipeline ran but API failed. Once fixed, regenerate content for those dates.
